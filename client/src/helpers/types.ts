export interface IFeedback {
    _id: string;
    title: string;
    category: string;
    description: string;
}

export interface IFeedbackArgs {
    title: string;
    category: string;
    description: string;
}

